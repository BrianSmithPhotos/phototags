[project]
name = "phototags"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pillow>=12.2.0",
    "pyexiftool>=0.5.6",
    "pyside6>=6.11.1",
]

[dependency-groups]
dev = [
    "py2app>=0.28.10",
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
